import os
import sys
import time


def run(self, filepath):
    line = 0
    AllFiles = os.listdir()
    for file in AllFiles:
        pass
        if os.path.isfile(f):
            pass
    print(x, type(x), type(x[0]))
    listOfFiles = [f for f in os.listdir() ]
    print(listOfFiles)

    return line


if __name__ == '__main__':
    run(None, "C:\\Users\\Admin\\Desktop\\HTML\\CuontLineOfCode")
